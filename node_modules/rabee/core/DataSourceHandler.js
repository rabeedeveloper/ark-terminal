((m,fs)=>{
  class _{
    constructor(project){
      this.path=project;
    }
    readConf(){
      //return eval(fs.readFileSync(this.path+"/conf/DataSource.json", 'utf8'));
      //let conf=fs.readFileSync(this.path+"/conf/DataSource.json", 'utf8').replace(/\n|\t|\r/g, "").replace(/\s+/g, ' ').trim();
      return require(this.path+"/conf/DataSource.json");
    }

  }



  m.exports=_;
})(module,require('fs'))
