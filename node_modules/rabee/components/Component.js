((m,ioc)=>{
  class _ {
    //ø : 248
    øAutowired(attrBId){
           this.øQualifier(attrBId,attrBId);
    }
    øQualifier(attr,beanId){

         this[attr]=ioc.RbFactory.getBean(beanId);
         var injectJob=setInterval(()=>{
           if(!this[attr]){
              this[attr]=ioc.RbFactory.getBean(beanId);
           }else{
             clearInterval(injectJob);
           }

         },1);
    }
  }

  m.exports=_;
})(module,require('../core/ioc.js'));
